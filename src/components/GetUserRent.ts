type GetUserRentProps = {
    username?: string,
    gameTitle: string,
    selectedPlan: string,
    profileType: string,
}
export function GetUserRent({ username, gameTitle, selectedPlan, profileType }: GetUserRentProps) {

    console.log(username,gameTitle, selectedPlan, profileType);
    return (

    )
}